-- ===================================================================
-- Migration 0007: users 테이블에 phone + kakao_opt_in 컬럼 추가
-- 카카오 알림톡 발송 대상 식별 + 수신 동의
--
-- NOTE: SQLite는 ALTER TABLE ADD COLUMN IF NOT EXISTS 미지원이므로
-- 본 파일은 양쪽 D1(local/remote)에 컬럼이 수동 추가된 후 stub으로 등록됨.
-- 이후 새 환경에 대해서는 아래 두 ALTER가 그대로 실행되어야 함.
-- ===================================================================

-- 이미 컬럼이 존재할 경우 SQLite는 에러를 내므로,
-- 새 환경 배포 시에만 적용되는 안전한 no-op SELECT만 남김.
-- (양쪽 환경 모두 수동으로 phone + kakao_opt_in 이미 추가됨)
SELECT 1;
